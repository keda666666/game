local server = require "server"
local lua_app = require "lua_app"

local UpdateVersion = {}

-- UpdateVersion[1] = function(conn)
-- 	print(conn:call_execute("select * from version"))
-- end

return UpdateVersion